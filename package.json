{
  "name": "starter-template",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "format": "prettier --write .",
    "build": "esbuild src/main.js --bundle --platform=node --format=cjs --outfile=dist/index.js",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "node-appwrite": "^14.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.2",
    "prettier": "^3.2.5"
  }
}
